<!-- [1.0.2] - 2025-09-18 - data/org.gtk.gtktext.gschema.xml
     Changed: Added GNOME HIG-compliant window sizing settings (width, height, maximized state)
-->
<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gtk.gtktext" path="/org/gtk/gtktext/">
    <key name="last-open-dir" type="s">
      <default>''</default>
      <summary>Last used directory for the Open dialog</summary>
      <description>
        The absolute filesystem path of the last directory successfully used
        in the Open File dialog. When set and valid, the dialog will start in
        this directory to improve user experience.
      </description>
    </key>
    <key name="heading-format" type="s">
      <default>'atx'</default>
      <summary>Heading format for markdown export</summary>
      <description>
        Format to use when exporting headings to markdown. 'atx' uses # symbols,
        'setext' uses underlines for H1 (=) and H2 (-) with ATX for H3-H6.
      </description>
    </key>
    <key name="autosave-enabled" type="b">
      <default>true</default>
      <summary>Enable automatic saving</summary>
      <description>
        When enabled, the application will automatically save changes every few seconds
        to prevent data loss. When disabled, only manual saves will occur.
      </description>
    </key>
    <key name="window-width" type="i">
      <default>900</default>
      <range min="600" max="3840"/>
      <summary>Main window width</summary>
      <description>
        Width of the main window in pixels. Automatically saved when the window
        is resized and restored on next launch. Minimum 600px for usability.
      </description>
    </key>
    <key name="window-height" type="i">
      <default>700</default>
      <range min="400" max="2160"/>
      <summary>Main window height</summary>
      <description>
        Height of the main window in pixels. Automatically saved when the window
        is resized and restored on next launch. Minimum 400px for usability.
      </description>
    </key>
    <key name="window-maximized" type="b">
      <default>false</default>
      <summary>Window maximized state</summary>
      <description>
        Whether the main window was maximized when last closed. Restored on
        next launch to maintain user's preferred window state.
      </description>
    </key>
    <key name="toolbar-style" type="s">
      <default>'icons'</default>
      <choices>
        <choice value='text'/>
        <choice value='icons'/>
        <choice value='both'/>
      </choices>
      <summary>Toolbar button display style</summary>
      <description>
        How toolbar buttons are displayed: 'text' shows text labels only,
        'icons' shows icons only, 'both' shows both icons and text labels.
        Icons mode uses GNOME Adwaita symbolic icons for consistency.
      </description>
    </key>
    <key name="toolbar-button-order" type="as">
      <default>['bold', 'italic', 'code', 'heading', 'hr', 'source']</default>
      <summary>Toolbar button order</summary>
      <description>
        Order of buttons in the toolbar. Only applies when toolbar-customization
        is enabled. Valid values: bold, italic, code, heading, hr, source.
      </description>
    </key>
    <key name="toolbar-customization" type="b">
      <default>false</default>
      <summary>Enable toolbar customization</summary>
      <description>
        When enabled, allows reordering toolbar buttons through preferences.
        When disabled, uses the default button order.
      </description>
    </key>
    <key name="version-history-enabled" type="b">
      <default>true</default>
      <summary>Enable version history</summary>
      <description>
        When enabled, the application will save previous versions of documents
        on each save operation. These can be restored later if needed.
      </description>
    </key>
    <key name="version-history-max-versions" type="i">
      <default>10</default>
      <range min="1" max="50"/>
      <summary>Maximum number of versions to keep</summary>
      <description>
        Maximum number of previous versions to keep per document. Older versions
        are automatically deleted when this limit is exceeded.
      </description>
    </key>
  </schema>
  <!-- Keep schema id and path in sync with application code and install path. -->
  <!-- Installed to: share/glib-2.0/schemas/org.gtk.gtktext.gschema.xml -->
</schemalist>
